# ==========================================================================
# LightRAG 服务配置（部署时复制为 deploy/lightrag/.env）
# 用法: cp .env.example .env 后修改【!】项
# 本文件挂载进 lightrag 容器 /app/.env（只读）
# 注意: 本 fork 开启了空间管理（SPACE_MANAGEMENT_ENABLED），VLM 必须用 VLM_LLM_* 命名
# ==========================================================================

# ---------- 服务基础 ----------
HOST=0.0.0.0
PORT=9621
WEBUI_TITLE='但问智能知识库'
WEBUI_DESCRIPTION="Simple and Fast Graph Based RAG System"
SPACE_MANAGEMENT_ENABLED=true

# ---------- 认证（【!】WebUI 直接暴露 9621 端口，必须配置） ----------
# rag-server 的 RAG_USERNAME/RAG_PASSWORD（deploy/.env）必须与这里 admin 的密码一致
AUTH_ACCOUNTS='admin:CHANGE_ME_lightrag_admin_password'
TOKEN_SECRET=CHANGE_ME_jwt_secret               # 【!】openssl rand -hex 32；不得使用默认值否则拒绝启动
JWT_ALGORITHM=HS256
TOKEN_EXPIRE_HOURS=48
# 也可再设一个 API Key（X-API-Key 头），可选：
# LIGHTRAG_API_KEY=

# ---------- LLM（DeepSeek） ----------
LLM_BINDING=openai
LLM_BINDING_HOST=https://api.deepseek.com/v1
LLM_BINDING_API_KEY=CHANGE_ME_deepseek_key      # 【!】
LLM_MODEL=deepseek-v4-flash

# ---------- Embedding（阿里云 DashScope，1024 维——与 Milvus 集合维度绑定，勿改模型） ----------
EMBEDDING_BINDING=openai
EMBEDDING_BINDING_HOST=https://dashscope.aliyuncs.com/compatible-mode/v1
EMBEDDING_BINDING_API_KEY=CHANGE_ME_dashscope_key   # 【!】
EMBEDDING_MODEL=text-embedding-v4
EMBEDDING_DIM=1024
EMBEDDING_TOKEN_LIMIT=8192
EMBEDDING_SEND_DIM=false
EMBEDDING_TIMEOUT=600

# ---------- Rerank（阿里云） ----------
RERANK_BINDING=aliyun
RERANK_MODEL=gte-rerank-v2
RERANK_BINDING_HOST=https://dashscope.aliyuncs.com/api/v1/services/rerank/text-rerank/text-rerank
RERANK_BINDING_API_KEY=CHANGE_ME_dashscope_key      # 【!】通常与 EMBEDDING 同一把 DashScope key
MIN_RERANK_SCORE=0.0
RERANK_BY_DEFAULT=True
RELATED_CHUNK_NUMBER=6

# ---------- VLM（图片解析，火山引擎；命名必须是 VLM_LLM_*） ----------
VLM_PROCESS_ENABLE=true
VLM_LLM_BINDING=openai
VLM_LLM_MODEL=doubao-seed-1-6-vision-250815
VLM_LLM_BINDING_HOST=https://ark.cn-beijing.volces.com/api/v3
VLM_LLM_BINDING_API_KEY=CHANGE_ME_volces_key        # 【!】

# ---------- 文档解析（Docling） ----------
LIGHTRAG_PARSER=pdf:docling
MULTIMODAL_PARSER=docling
DOCLING_ENDPOINT=http://docling:5001              # compose 服务名，勿改
DOCLING_DO_OCR=true

# ---------- 检索与分块调优 ----------
TOP_K=40
CHUNK_TOP_K=20
MAX_ENTITY_TOKENS=6000
MAX_RELATION_TOKENS=8000
MAX_TOTAL_TOKENS=30000
KG_CHUNK_PICK_METHOD=VECTOR
ENABLE_LLM_CACHE_FOR_EXTRACT=true
SUMMARY_LANGUAGE=Chinese
CHUNK_SIZE=1200
CHUNK_OVERLAP_SIZE=100
MAX_EXTRACT_INPUT_TOKENS=61440
WORKER_TIMEOUT=300
FUNC_TIMEOUT=300
MAX_ASYNC=4
MAX_PARALLEL_INSERT=2

# ---------- 存储后端（全部指向 compose 服务） ----------
LIGHTRAG_KV_STORAGE=RedisKVStorage
LIGHTRAG_DOC_STATUS_STORAGE=PGDocStatusStorage
LIGHTRAG_GRAPH_STORAGE=Neo4JStorage
LIGHTRAG_VECTOR_STORAGE=MilvusVectorDBStorage

REDIS_URI=redis://redis:6379
REDIS_SOCKET_TIMEOUT=50
REDIS_CONNECT_TIMEOUT=60
REDIS_MAX_CONNECTIONS=100
REDIS_RETRY_ATTEMPTS=3

POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=CHANGE_ME_pg_password           # 【!】必须与 deploy/.env 的 POSTGRES_PASSWORD 一致
POSTGRES_DATABASE=lightrag                        # 由 postgres initdb 脚本自动创建
POSTGRES_MAX_CONNECTIONS=12
POSTGRES_ENABLE_VECTOR=false

NEO4J_URI=neo4j://neo4j:7687
NEO4J_USERNAME=neo4j
NEO4J_PASSWORD=CHANGE_ME_neo4j_password           # 【!】必须与 deploy/.env 的 NEO4J_PASSWORD 一致

MILVUS_URI=http://milvus:19530
MILVUS_DB_NAME=lightrag
MILVUS_INDEX_TYPE=AUTOINDEX
MILVUS_METRIC_TYPE=COSINE
